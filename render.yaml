services:
  - type: web
    name: loan-frontend
    runtime: static
    branch: main
    buildCommand: npm ci && npm run build
    staticPublishPath: ./out
    envVars:
      - key: NEXT_PUBLIC_API_URL
        sync: false
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
